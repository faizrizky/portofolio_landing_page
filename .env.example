# URL tempat repo admin-panel di-deploy. Landing page ini fetch data
# project & profile dari API publiknya (GET /api/projects, GET /api/profile).
# Dev lokal, kalau admin-panel jalan di localhost:3000, samakan port ini beda,
# misal jalankan landing page di localhost:3001.
NEXT_PUBLIC_API_URL="http://localhost:3000"
